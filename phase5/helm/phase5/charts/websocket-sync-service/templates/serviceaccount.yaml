apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "websocket-sync-service.serviceAccountName" . }}
  namespace: {{ .Values.global.namespace | default "phase5" }}
  labels:
    {{- include "websocket-sync-service.labels" . | nindent 4 }}
