apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "chat-api.serviceAccountName" . }}
  namespace: {{ .Values.global.namespace | default "phase5" }}
  labels:
    {{- include "chat-api.labels" . | nindent 4 }}
